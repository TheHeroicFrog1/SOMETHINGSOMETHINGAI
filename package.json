{
  "name": "bifrost",
  "version": "1.0.0",
  "description": "Bifrost Multi-Agent AI Interface",
  "author": "FROGGY <froggy@example.com>",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend:win": "cd backend && python -m PyInstaller --name bifrost-backend --noconsole --onefile main.py",
    "build:backend:mac": "cd backend && python -m PyInstaller --name bifrost-backend --onefile main.py",
    "build:backend:linux": "cd backend && python -m PyInstaller --name bifrost-backend --onefile main.py",
    "package:win": "npm run build:frontend && npm run build:backend:win && electron-builder --win",
    "package:mac": "npm run build:frontend && npm run build:backend:mac && electron-builder --mac",
    "package:linux": "npm run build:frontend && npm run build:backend:linux && electron-builder --linux",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "build": {
    "appId": "com.bifrost.app",
    "productName": "Bifrost",
    "directories": {
      "output": "release"
    },
    "files": [
      "main.js",
      "frontend/dist/**/*",
      "build/**/*"
    ],
    "extraResources": [
      {
        "from": "backend/dist",
        "to": "backend-dist",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "target": [
        "nsis",
        "zip"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "identity": null
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ]
    }
  },
  "devDependencies": {
    "electron": "^29.4.6",
    "electron-builder": "^24.9.1",
    "vitepress": "^1.6.4"
  }
}
